services:
  web:
    build:
      target: development

    ports:
      - "8000:8000"

    volumes:
      - .:/workspace

    environment:
      POSTGRES_HOST: db

    command: >
      uv run python manage.py runserver 0.0.0.0:8000

  db:
    ports:
      - "5432:5432"
